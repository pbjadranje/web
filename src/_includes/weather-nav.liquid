Vremenska napoved na dan:
<select id="forecastSelector" class="text-primary border border-gray-300 rounded-md p-2">
    {% for image in collections[route] reversed %}
        {% assign parts = image.name | split: "." %}
        <option value="{{ image.name }}">{{ parts[0] }}</option>
    {% endfor %}
</select>
